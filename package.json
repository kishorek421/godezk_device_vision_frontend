{
  "name": "godezk-telemetry-dashboard-frontend",
  "version": "1.0.0",
  "description": "Telemetry Dashboard Frontend — Executions list and analytics views",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "start": "npx serve dist"
  },
  "engines": {
    "node": ">=20"
  }
}
